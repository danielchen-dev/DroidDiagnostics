# DroidDiagnostics v0.3.5 Release Patch

Patch contents:

- Admin logout endpoint
- Admin dashboard logout action
- Release version 0.3.5
- Environment password documentation

Apply by copying files over v0.2.0.

Before deploy:

```bash
npm run check
npm run build
```
